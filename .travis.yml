language: java
sudo: required
jdk:
  - oraclejdk8
install:
  - sudo apt-get update && sudo apt-get install oracle-java8-installer
  - java -version
cache:
  directories:
    - $HOME/.gradle
